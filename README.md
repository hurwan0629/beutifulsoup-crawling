# beutifulsoup를 이용한 네이브 증권 주요뉴스 크롤링

## 실행방법
1. 프로젝트 다운
2. `python -m venv .venv` 를 통한 venv 환경 마련
3. `pip install -r requirements.txt` 를 통한 라이브러리 다운
4. 실행

위 작업을 하면 Npay의 증권 사이트에서 크롤링이 진행되어 "뉴스크롤링 - YYYY-MM-DD.txt" 파일이 생성되게 됩니다.
